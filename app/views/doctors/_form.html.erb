<%= form_for(@doctor) do |f| %>
  <% if @doctor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doctor.errors.count, "error") %> prohibited this doctor from being saved:</h2>

      <ul>
      <% @doctor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= render partial: "shared/person_form", locals: { form: f, person: @doctor } %>

  <div class="field">
    <%= f.label :specialty %><br>
    <%= f.select :specialty_id, options_from_collection_for_select(Specialty.all, :id, :name, @doctor.specialty_id), include_blank: 'Select specialty' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
